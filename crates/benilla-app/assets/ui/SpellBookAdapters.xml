<Ui>
    <!--
        Deviation: the pet page's autocast widgets take the pet bar button's proportions, so the
        shine and the corner brackets sit on the button's edge and share its centre. Stock
        SpellBookFrame.xml has the shine 36x36 at CENTER (1,1), scale 1.22, the brackets 60; 71.53
        is 37 x 58/30 (the art fills 33/64 of UI-AutoCastableOverlay.blp).
    -->
    <Script><![CDATA[
        for i = 1, 12 do
            local button = getglobal("SpellButton" .. i)
            local overlay = getglobal("SpellButton" .. i .. "AutoCastable")
            overlay:SetWidth(71.53)
            overlay:SetHeight(71.53)
            local pane = getglobal("SpellButton" .. i .. "AutoCast")
            pane:SetWidth(37)
            pane:SetHeight(37)
            pane:ClearAllPoints()
            pane:SetPoint("CENTER", button, "CENTER", 0, 0)
            pane:SetModelScale(1.48)
        end
    ]]></Script>
</Ui>
